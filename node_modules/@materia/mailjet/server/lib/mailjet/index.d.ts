import { App } from '@materia/server';
export declare class MailjetSender {
    private app;
    static fromName: string;
    static from: string;
    static apikey: string;
    static secret: string;
    mailjet: any;
    readonly settingsHasChanged: boolean;
    constructor(app: App);
    reload(): void;
    init(): void;
    connect(key: any, secret: any): void;
    send(params: any): any;
    sendTemplate(params: any): any;
}

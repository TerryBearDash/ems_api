import { OnInit, EventEmitter } from '@angular/core';
import { FormGroup, FormBuilder } from '@angular/forms';
import { HttpClient } from '@angular/common/http';
export interface IUserManagementSetup {
    method: string;
    user_profile_enabled: boolean;
    user_profile_entity: string;
    email_verification: boolean;
    email_addon: string;
}
export declare class UserManagementSetupComponent implements OnInit {
    private fb;
    private http;
    app: any;
    settings: IUserManagementSetup;
    baseUrl: string;
    token: string;
    save: EventEmitter<IUserManagementSetup>;
    cancel: EventEmitter<void>;
    loginForm: FormGroup;
    entities: any[];
    emailAddons: any[];
    constructor(fb: FormBuilder, http: HttpClient);
    private getSettingsProperty(property, defaultValue);
    ngOnInit(): void;
    finish(): void;
    close(): void;
}

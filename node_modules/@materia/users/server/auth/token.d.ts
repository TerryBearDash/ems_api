import { Auth } from './auth';
export declare class TokenAuth extends Auth {
    constructor(app: any, config: any);
    verifyToken(accessToken: any, done: any): any;
    clearExpiredTokens(): void;
}

import { Auth } from './auth/auth';
export default class UserManagementAddon {
    private app;
    private config;
    private express;
    static displayName: string;
    static logo: string;
    static installSettings: boolean;
    signupParams: any[];
    options: {
        history: boolean;
        save: boolean;
        db: boolean;
    };
    disabled: boolean;
    auth: Auth;
    constructor(app: any, config: any, express: any);
    afterLoadEntities(): Promise<void> | Promise<any[]>;
    beforeLoadQueries(): any;
    afterLoadQueries(): Promise<any>;
    afterLoadAPI(): void | Promise<void>;
    start(): Promise<void>;
    uninstall(app: any): void;
}

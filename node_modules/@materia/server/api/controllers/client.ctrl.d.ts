import { App } from '../../lib';
import { WebsocketInstance } from '../../lib/websocket';
import { PackageManager } from '../../lib/package-manager';
export declare class ClientController {
    private app;
    private websocket;
    packageManager: PackageManager;
    proc: any;
    constructor(app: App, websocket: WebsocketInstance);
    install(req: any, res: any): Promise<any>;
    installAll(req: any, res: any): Promise<any>;
    startWatching(req: any, res: any): Promise<void>;
    stopWatching(req: any, res: any): void;
    build(req: any, res: any): void;
    private _parseProgress;
    private _parseEnd;
    private _parseErrors;
    private _kill;
    private getPkgFromRequest;
}

import { IPermission } from '@materia/interfaces';
import { App } from '../../lib';
import { WebsocketInstance } from '../../lib/websocket';
export declare class PermissionsLib {
    static list(app: App): IPermission[];
}
export declare class PermissionsController {
    private app;
    constructor(app: App, websocket: WebsocketInstance);
    add(req: any, res: any): any;
    get(req: any, res: any): any;
    list(req: any, res: any): void;
    remove(req: any, res: any): any;
    update(req: any, res: any): any;
    private _checkNewPermission;
    private _getDefaultMiddleware;
    private _getPermissionFilepath;
}

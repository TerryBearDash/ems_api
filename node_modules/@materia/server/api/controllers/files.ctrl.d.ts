import { Application as ExpressApplication } from 'express';
import { App } from '../../lib';
import { WebsocketInstance } from '../../lib/websocket';
export declare class FilesController {
    private app;
    readonly api: ExpressApplication;
    constructor(app: App, websocket: WebsocketInstance);
    getFullPath(relativePath: any): string;
    read(req: any, res: any): void;
    write(req: any, res: any): void;
    move(req: any, res: any): void;
    isDirectory(req: any, res: any): void;
    remove(req: any, res: any): void;
}

import { App } from '../../lib';
import { WebsocketInstance } from '../../lib/websocket';
export declare class AppController {
    private app;
    constructor(app: App, websocket: WebsocketInstance);
    entitySpacing: number;
    restart(req: any, res: any): void;
    private saveServerSettings;
    private saveDatabaseSettings;
    private saveClientSettings;
    config(req: any, res: any): void;
    deleteConfig(req: any, res: any): void;
    search(req: any, res: any): void;
    createDockerfile(req: any, res: any): void;
    getMinimalInfo(req: any, res: any): void;
    getInfos(req: any, res: any): void;
}

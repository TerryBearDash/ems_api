import { App } from '../../lib';
import { WebsocketInstance } from '../../lib/websocket';
export declare class AddonsController {
    private app;
    constructor(app: App, websocket: WebsocketInstance);
    getConfig(req: any, res: any): void;
    setup(req: any, res: any): void;
    enable(req: any, res: any): void;
    disable(req: any, res: any): void;
    bundle(req: any, res: any): Promise<any>;
    private getPkgFromRequest;
}

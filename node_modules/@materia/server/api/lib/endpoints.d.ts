import { IEndpoint, IParam } from '@materia/interfaces';
export declare class EndpointsLib {
    static list(app: any): IEndpoint[];
    static generate(app: any, entity: any, method: any, id: any): void;
    static cleanParams(params: IParam[]): IParam[];
}

import { App } from '../../lib';
export declare class VueCli {
    private app;
    config: any;
    constructor(app: App);
    execVue(command: string, params?: string[]): Promise<any>;
    execVueCliService(command: string, params?: string[], cwd?: string): Promise<any>;
}

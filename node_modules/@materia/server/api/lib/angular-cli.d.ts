import { App } from '../../lib';
export declare class AngularCli {
    private app;
    config: any;
    constructor(app: App);
    exec(command: string, params?: string[], cwd?: string): Promise<string>;
    getConfig(): Promise<{}>;
    initNewMonopackageConfig(projectName: string): Promise<{}>;
    saveConfig(): Promise<any>;
}

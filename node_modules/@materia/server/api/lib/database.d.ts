import { IEntity } from '@materia/interfaces';
import { DBEntity } from '../../lib/entities/db-entity';
import { App } from '../../lib/app';
export declare class DatabaseLib {
    static entitySpacing: number;
    static generateActionId({ stringBase, byteLength }?: {
        stringBase?: string;
        byteLength?: number;
    }): Promise<string>;
    static loadEntityJson(entity: DBEntity): IEntity;
    static loadEntitiesJson(app: App): IEntity[];
    private static entityWidth;
    private static entityHeight;
    private static getWorkspaceSize;
    static isAvailableSpace(entities: any, entity: any, pX: any, pY: any): boolean;
    static loadUi(p: any, entities: any, entityName: any): {
        x: number;
        y: number;
    };
}

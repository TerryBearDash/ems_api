import { App } from '../lib';
export declare class OAuth {
    private app;
    server: any;
    tokens: Array<{
        username: string;
        expires_in: Date;
        token: string;
        scope: string[];
    }>;
    readonly token: any[] | ((req: any, res: any, next: any) => any);
    readonly isAuth: any;
    constructor(app: App);
    initialize(): void;
    static getTokenFromHeaders(req: any): string;
    verifyLogin(clientId: any, clientSecret: any, done: any): any;
    verifyToken(accessToken: any, done: any): any;
    private clearExpiredTokens;
    private generateToken;
}

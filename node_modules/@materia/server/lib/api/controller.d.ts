import { App } from '../app';
export declare class Controller {
    private app;
    ctrlClass: any;
    ctrlStr: string;
    ctrlInstance: any;
    constructor(app: App);
    load(basePath: string, controller: string): void;
    instance(): any;
}

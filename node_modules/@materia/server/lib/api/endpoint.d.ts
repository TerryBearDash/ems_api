import { IEndpoint, IParam } from '@materia/interfaces';
import { App } from '../app';
import { Controller } from './controller';
import { Entity } from '../entities/entity';
import { Query } from '../entities/query';
export declare enum Method {
    GET = 0,
    POST = 1,
    PUT = 2,
    DELETE = 3,
    PATCH = 4
}
export declare class Endpoint {
    private app;
    name: string;
    method: string;
    url: string;
    fromAddon?: IEndpoint['fromAddon'];
    entity: Entity;
    params: Array<IParam>;
    data: Array<IParam>;
    permissions: Array<string>;
    controller: string;
    action: string;
    query: Query;
    queryStr: string;
    static controllers: {
        [name: string]: Controller;
    };
    static resetControllers(): void;
    constructor(app: App, endpointConfig: IEndpoint);
    getParam(name: string): IParam;
    getData(name: string): IParam;
    getAllData(onlyRequired?: boolean): IParam[];
    getAllParams(onlyRequired?: boolean): IParam[];
    getRequiredParams(): IParam[];
    getRequiredData(): IParam[];
    handle(req: any, res: any, next: any): Promise<any>;
    isInUrl(name: any): boolean;
    toJson(): IEndpoint;
    private _getController;
    private _buildParams;
    private _handleParams;
    private _handleHttp;
    private _handleWS;
}

import { IPermission } from '@materia/interfaces';
import { App } from '../app';
export declare class Permission {
    app: App;
    name: string;
    description: string;
    readOnly: boolean;
    middleware: any;
    file: string;
    invalid: boolean;
    fromAddon: IPermission['fromAddon'];
    constructor(app: any, data: IPermission);
    reload(): void;
    toJson(): IPermission;
}

import * as WS from 'ws';
import { App } from '../lib';
export declare type IWebsocketVerifyClient = (info: {
    origin: any;
    secure: any;
    req: any;
}, cb: (done: boolean) => any) => any;
export declare class WebsocketInstance {
    instance: WS.Server;
    constructor(verifyClient?: IWebsocketVerifyClient);
    broadcast(data: any): void;
}
export declare class WebsocketServers {
    private app;
    servers: {
        [path: string]: WebsocketInstance;
    };
    constructor(app: App);
    get(endpoint: string): WebsocketInstance;
    register(endpoint: string, verifyClient?: IWebsocketVerifyClient): WebsocketInstance;
    close(): void;
}

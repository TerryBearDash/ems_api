import { IDatabaseDiffs, IApplyOptions, IActionData } from '@materia/interfaces';
import { App } from './app';
export declare class Synchronizer {
    private app;
    constructor(app: App);
    diff(): Promise<IDatabaseDiffs>;
    entitiesToDatabase(diffs: IDatabaseDiffs, options?: IApplyOptions): Promise<IActionData[]>;
    databaseToEntities(diffs: IDatabaseDiffs, options?: IApplyOptions): Promise<IActionData[]>;
    private _compareField;
    private _constructEntitiesDiffs;
    private _sortCreateEntitiesDiffsWithRelations;
    private _compareRelation;
    private _diffRelation;
    private _diffEntity;
    private _diffEntities;
    private _diffMap;
}

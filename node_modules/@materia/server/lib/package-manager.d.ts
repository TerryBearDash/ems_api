import { Npm } from './package-manager/npm';
import { Yarn } from './package-manager/yarn';
export declare class PackageManager {
    private basepath;
    nodeModuleManager: Npm | Yarn;
    managerName: string;
    constructor(basepath: string);
    runScriptInBackground(scriptName: any, cwd?: string): Promise<{
        proc: any;
    }>;
    runScript(scriptName: string, cwd?: string, stream?: (data: any, error?: boolean) => void): Promise<any>;
    uninstall(packageName: string, stream?: (data: any, error?: boolean) => void): Promise<any>;
    upgrade(packageName: string, stream?: (data: any, error?: boolean) => void): Promise<any>;
    install(packageName: string, stream?: (data: any, error?: boolean) => void): Promise<any>;
    installAll(stream?: (data: any, error?: boolean) => void): Promise<any>;
    installAllInBackground(cwd?: string): Promise<{
        proc: any;
    }>;
    getPackageManagerName(): string;
    getPackageJson(): Promise<any>;
    getDependencies(): Promise<any>;
    getDevdependencies(): Promise<any>;
    getScripts(): Promise<any>;
    hasNodeModules(): boolean;
}

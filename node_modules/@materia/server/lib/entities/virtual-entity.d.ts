import { IVirtualEntityConfig } from '@materia/interfaces';
import { App } from '../app';
import { Entity } from './entity';
export declare class VirtualEntity extends Entity {
    reservedQueries: string[];
    model: any;
    constructor(app: App);
    generateDefaultQueries(): void;
    loadModel(): Promise<void>;
    toJson(): IVirtualEntityConfig;
}

import { Query } from '../query';
export declare class CreateQuery extends Query {
    type: string;
    opts: any;
    values: any;
    valuesType: any;
    constructor(entity: any, id: any, opts: any);
    refresh(): void;
    discoverParams(): void;
    resolveParams(params: any): Promise<{}>;
    run(params: any, options: any): Promise<any>;
    toJson(): {
        id: string;
        type: string;
        opts: {
            values: any;
        };
    };
}

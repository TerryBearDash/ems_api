import { IValidator } from '@materia/interfaces';
import { Field } from './field';
export declare class Validator {
    private field;
    name: string;
    value: any;
    constructor(field: Field, name: string, value: any);
    toJson(): IValidator;
}

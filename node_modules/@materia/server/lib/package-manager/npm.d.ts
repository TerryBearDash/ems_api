export declare class Npm {
    private cwd;
    constructor(cwd: string);
    execInBackground(command: string, params?: string[], cwd?: string): Promise<{
        proc: any;
    }>;
    exec(command: string, params?: string[], cwd?: string, stream?: (data: any, error?: boolean) => void): Promise<any>;
    private getNpmPath;
}

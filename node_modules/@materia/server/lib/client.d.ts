import { IClientBuild } from '@materia/interfaces';
import { App } from './app';
export declare enum ScriptMode {
    WATCH,
    BUILD,
    PROD
}
export declare class Client {
    private app;
    config: IClientBuild;
    pkgPath: string;
    pkgScripts: string[];
    watching: boolean;
    constructor(app: App);
    load(): Promise<void>;
    hasOneScript(): boolean;
    hasBuildScript(mode?: ScriptMode, script?: string): boolean;
}

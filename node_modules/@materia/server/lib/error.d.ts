import { IErrorOptions } from '@materia/interfaces';
export declare class MateriaError extends Error {
    slug: string;
    issue: number;
    debug: string;
    originalError?: Error;
    constructor(message: any, options?: IErrorOptions);
    toString(): string;
}

import { App } from './app';
import { DBEntity } from './entities/db-entity';
export declare class Session {
    private app;
    constructor(app: App);
    initialize(): Promise<any>;
    setupMiddleware(dbEntity?: DBEntity): void;
}
